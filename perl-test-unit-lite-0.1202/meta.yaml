package:
  name: perl-test-unit-lite
  version: !!str 0.1202

source:
  fn: Test-Unit-Lite-0.1202.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DE/DEXTER/Test-Unit-Lite-0.1202.tar.gz
  md5: 2ccb9d4fe1d702c5b88838d91abfe3e3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl

  run:
    - perl

test:
  # Perl 'use' tests
  # imports:

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Test-Unit-Lite
  license: perl_5
  summary: 'Unit testing without external dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
