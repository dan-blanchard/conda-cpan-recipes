package:
  name: perl-clipboard
  version: !!str 0.13

source:
  fn: Clipboard-0.13.tar.gz
  url: http://cpan.metacpan.org/authors/id/K/KI/KING/Clipboard-0.13.tar.gz
  md5: 691e17df1d4c074284c85abac6c0c973
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

test:
  # Perl 'use' tests
  # imports:

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Clipboard
  license: perl_5
  summary: 'Cliboard - Copy and Paste with any OS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
