package:
  name: perl-datetime
  version: !!str 1.07

source:
  fn: DateTime-1.07.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DR/DROLSKY/DateTime-1.07.tar.gz
  md5: 2b1745e4f7c0777c02545033588f6394
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-cpan-changes 0.19 # CPAN-Changes
    - perl-test-notabs # Test-NoTabs
    - perl-test-pod 1.41 # Test-Pod
    - perl-test-fatal # Test-Fatal

  run:
    - perl
    - perl-try-tiny # Try-Tiny
    - perl-params-validate 0.76 # Params-Validate
    - perl-datetime-timezone 1.09 # DateTime-TimeZone
    - perl-datetime-locale 0.41 # DateTime-Locale

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/DateTime
  license: artistic_2
  summary: 'A date and time object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
