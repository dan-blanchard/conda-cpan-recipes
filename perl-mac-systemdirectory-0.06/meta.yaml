package:
  name: perl-mac-systemdirectory
  version: !!str 0.06

source:
  fn: Mac-SystemDirectory-0.06.tar.gz
  url: http://cpan.metacpan.org/authors/id/C/CH/CHANSEN/Mac-SystemDirectory-0.06.tar.gz
  md5: e24ce731bef85021a5a0334b2773c5cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl
    - perl-extutils-makemaker
    - perl-test-simple

  run:
    - perl

test:
  # Perl 'use' tests
  imports:
    - "Mac::SystemDirectory qw[:all]"

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Mac-SystemDirectory
  license: perl_5
  summary: 'Locate Mac OS X Standard System Directories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
