package:
  name: perl-class-method-modifiers
  version: !!str 2.09

source:
  fn: Class-Method-Modifiers-2.09.tar.gz
  url: http://cpan.metacpan.org/authors/id/E/ET/ETHER/Class-Method-Modifiers-2.09.tar.gz
  md5: 5adc748acd8670b71ceaf73d48446dcf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-module-build-tiny # Module-Build-Tiny
    - perl-test-fatal # Test-Fatal

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moose/Class-Method-Modifiers
  license: p
  summary: 'provides Moose-like method modifiers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
