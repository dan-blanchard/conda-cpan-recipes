package:
  name: perl-test-cleannamespaces
  version: !!str 0.05

source:
  fn: Test-CleanNamespaces-0.05.tar.gz
  url: http://cpan.metacpan.org/authors/id/E/ET/ETHER/Test-CleanNamespaces-0.05.tar.gz
  md5: 9d09f173aaba94a2e5759e1b9838c930
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-sub-name # Sub-Name
    - perl-module-runtime # Module-Runtime
    - perl-moose # Moose
    - perl-file-find-rule # File-Find-Rule
    - perl-namespace-autoclean # namespace-autoclean
    - perl-file-find-rule-perl # File-Find-Rule-Perl
    - perl-test-checkdeps # Test-CheckDeps
    - perl-test-tester # Test-Tester
    - perl-sub-exporter # Sub-Exporter

  run:
    - perl
    - perl-sub-name # Sub-Name
    - perl-moose # Moose
    - perl-file-find-rule-perl # File-Find-Rule-Perl
    - perl-file-find-rule # File-Find-Rule
    - perl-sub-exporter # Sub-Exporter
    - perl-module-runtime # Module-Runtime
    - perl-namespace-autoclean # namespace-autoclean

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karenetheridge/Test-CleanNamespaces
  license: perl_5
  summary: 'Check for uncleaned imports'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
