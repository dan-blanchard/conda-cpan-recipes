package:
  name: perl-params-check
  version: !!str 0.36

source:
  #fn: Params-Check-0.36.tar.gz
  #url: http://cpan.metacpan.org/authors/id/B/BI/BINGOS/Params-Check-0.36.tar.gz
  #md5: 3bf19a6ca0eb1e4f2740b791ca890813
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

test:
  # Perl 'use' tests
  # imports:

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Params-Check
  license: perl_5
  summary: 'Templated based param validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
