package:
  name: perl-datetime-format-strptime
  version: !!str 1.54

source:
  fn: DateTime-Format-Strptime-1.54.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DR/DROLSKY/DateTime-Format-Strptime-1.54.tar.gz
  md5: ca0fa912861f360f9aa535c6b4c0282f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-pod 1.41 # Test-Pod

  run:
    - perl
    - perl-params-validate 0.64 # Params-Validate
    - perl-datetime-locale 0.45 # DateTime-Locale
    - perl-datetime-timezone 0.79 # DateTime-TimeZone
    - perl-datetime 1.00 # DateTime

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/DateTime-Format-Strptime
  license: artistic_2
  summary: 'Parse and format strp and strf time patterns'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
