package:
  name: perl-path-isdev
  version: !!str 1.000002

source:
  fn: Path-IsDev-1.000002.tar.gz
  url: http://cpan.metacpan.org/authors/id/K/KE/KENTNL/Path-IsDev-1.000002.tar.gz
  md5: 5645c9006031cc1fed8dce70095e312e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-class-tiny # Class-Tiny
    - perl-role-tiny # Role-Tiny
    - perl-test-fatal # Test-Fatal
    - perl-path-tiny # Path-Tiny
    - perl-sub-exporter # Sub-Exporter
    - perl-module-runtime # Module-Runtime
    - perl-file-homedir # File-HomeDir

  run:
    - perl
    - perl-class-tiny # Class-Tiny
    - perl-role-tiny # Role-Tiny
    - perl-path-tiny # Path-Tiny
    - perl-file-homedir # File-HomeDir
    - perl-sub-exporter # Sub-Exporter
    - perl-module-runtime # Module-Runtime

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kentfredric/Path-IsDev
  license: perl_5
  summary: 'Determine if a given Path resembles a development source tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
