package:
  name: perl-package-stash
  version: !!str 0.36

source:
  fn: Package-Stash-0.36.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DO/DOY/Package-Stash-0.36.tar.gz
  md5: 066aa5099af88457c4f1eb2448272061
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-leaktrace # Test-LeakTrace
    - perl-test-fatal # Test-Fatal
    - perl-test-simple 0.88 # Test-Simple
    - perl-extutils-makemaker 6.30 # ExtUtils-MakeMaker
    - perl-dist-checkconflicts 0.02 # Dist-CheckConflicts
    - perl-pod-coverage-trustpod # Pod-Coverage-TrustPod
    - perl-test-requires # Test-Requires
    - perl-test-pod 1.41 # Test-Pod
    - perl-test-pod-coverage 1.08 # Test-Pod-Coverage

  run:
    - perl
    - perl-dist-checkconflicts 0.02 # Dist-CheckConflicts
    - perl-module-implementation 0.06 # Module-Implementation

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/release/Package-Stash
  license: perl_5
  summary: 'routines for manipulating stashes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
