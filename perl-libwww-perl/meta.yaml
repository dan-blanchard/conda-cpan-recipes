package:
  name: perl-libwww-perl
  version: !!str 6.05

source:
  fn: libwww-perl-6.05.tar.gz
  url: http://cpan.metacpan.org/authors/id/G/GA/GAAS/libwww-perl-6.05.tar.gz
  md5: 637d5f1eb61336ca2caa6e026b382f87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl
    - perl-html-parser # HTML-Parser
    - perl-encode-locale # Encode-Locale
    - perl-file-listing 6 # File-Listing
    - perl-http-daemon 6 # HTTP-Daemon
    - perl-net-http 6.04 # Net-HTTP
    - perl-http-cookies 6 # HTTP-Cookies
    - perl-uri # URI
    - perl-lwp-mediatypes 6 # LWP-MediaTypes
    - perl-http-message 6 # HTTP-Message
    - perl-www-robotrules 6 # WWW-RobotRules
    - perl-http-negotiate 6 # HTTP-Negotiate
    - perl-http-date 6 # HTTP-Date
    - perl-uri 1.10 # URI

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/libwww-perl
  license: perl_5
  summary: 'The World-Wide Web library for Perl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
