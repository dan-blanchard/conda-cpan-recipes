package:
  name: perl-extutils-command
  version: !!str 1.17

source:
  #fn: ExtUtils-Command-1.17.tar.gz
  #url: http://cpan.metacpan.org/authors/id/F/FL/FLORA/ExtUtils-Command-1.17.tar.gz
  #md5: 8d2bd6a2311b6264d3dd96c11601c34a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

test:
  # Perl 'use' tests
  # imports:

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://search.cpan.org/dist/ExtUtils-Command
  license: perl_5
  summary: 'utilities to replace common UNIX commands in Makefiles etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
