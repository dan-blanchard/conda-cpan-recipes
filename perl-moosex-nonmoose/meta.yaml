package:
  name: perl-moosex-nonmoose
  version: !!str 0.25

source:
  fn: MooseX-NonMoose-0.25.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DO/DOY/MooseX-NonMoose-0.25.tar.gz
  md5: 0f0b7aca5fd58dea8787fb1bda498166
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-module-runtime # Module-Runtime
    - perl-moose # Moose
    - perl-list-moreutils # List-MoreUtils
    - perl-test-fatal # Test-Fatal

  run:
    - perl
    - perl-module-runtime # Module-Runtime
    - perl-moose # Moose
    - perl-list-moreutils # List-MoreUtils

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/release/MooseX-NonMoose
  license: perl_5
  summary: 'easy subclassing of non-Moose classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
