package:
  name: perl-namespace-clean
  version: !!str 0.24

source:
  fn: namespace-clean-0.24.tar.gz
  url: http://cpan.metacpan.org/authors/id/R/RI/RIBASUSHI/namespace-clean-0.24.tar.gz
  md5: bb6f2e2bba5f028a0e939cc6f2639fba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl
    - perl-b-hooks-endofscope 0.12 # B-Hooks-EndOfScope
    - perl-package-stash 0.23 # Package-Stash

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://search.cpan.org/dist/namespace-clean
  license: perl_5
  summary: 'Keep imports and functions out of your namespace'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
