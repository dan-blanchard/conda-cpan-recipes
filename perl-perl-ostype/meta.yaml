package:
  name: perl-perl-ostype
  version: !!str 1.007

source:
  fn: Perl-OSType-1.007.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/Perl-OSType-1.007.tar.gz
  md5: 7b0795b82471322ec32d267c333dc895
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-cpan-meta # Test-CPAN-Meta
    - perl-test-simple 0.88 # Test-Simple
    - perl-dist-zilla-pluginbundle-dagolden 0.060 # Dist-Zilla-PluginBundle-DAGOLDEN
    - perl-extutils-makemaker 6.17 # ExtUtils-MakeMaker
    - perl-pod-coverage-trustpod # Pod-Coverage-TrustPod
    - perl-dist-zilla-plugin-onlycoreprereqs 0.003 # Dist-Zilla-Plugin-OnlyCorePrereqs
    - perl-test-pod 1.41 # Test-Pod
    - perl-test-pod-coverage 1.08 # Test-Pod-Coverage
    - perl-dist-zilla 5.011 # Dist-Zilla

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dagolden/Perl-OSType
  license: perl_5
  summary: 'Map Perl operating system names to generic types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
