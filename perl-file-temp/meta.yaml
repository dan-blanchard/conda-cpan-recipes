package:
  name: perl-file-temp
  version: !!str 0.2304

source:
  fn: File-Temp-0.2304.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/File-Temp-0.2304.tar.gz
  md5: c1e19bfcc8cfc440b149a108be54b9d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-cpan-meta # Test-CPAN-Meta
    - perl-pod-coverage-trustpod # Pod-Coverage-TrustPod
    - perl-test-pod-coverage 1.08 # Test-Pod-Coverage
    - perl-test-pod 1.41 # Test-Pod
    - perl-extutils-makemaker 6.17 # ExtUtils-MakeMaker

  run:
    - perl
    - perl-file-path 2.06 # File-Path
    - perl-exporter 5.57 # Exporter
    - perl-parent 0.221 # parent
    - perl-pathtools 0.8 # PathTools

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Perl-Toolchain-Gang/File-Temp
  license: perl_5
  summary: 'return name and handle of a temporary file safely'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
