package:
  name: perl-regexp-common
  version: !!str 2013031301

source:
  fn: Regexp-Common-2013031301.tar.gz
  url: http://cpan.metacpan.org/authors/id/A/AB/ABIGAIL/Regexp-Common-2013031301.tar.gz
  md5: 2b9c335312dcfd9980ff7acbad0e5905
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl
    - perl-extutils-makemaker

  run:
    - perl

test:
  # Perl 'use' tests
  # imports:

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Regexp-Common
  license: mit
  summary: 'Provide commonly requested regular expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
