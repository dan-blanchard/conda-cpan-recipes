package:
  name: perl-algorithm-c3
  version: !!str 0.08

source:
  fn: Algorithm-C3-0.08.tar.gz
  url: http://cpan.metacpan.org/authors/id/F/FL/FLORA/Algorithm-C3-0.08.tar.gz
  md5: 07afb08682125c875b80c315d99f6f95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Algorithm-C3
  license: perl_5
  summary: 'A module for merging hierarchies using the C3 algorithm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
