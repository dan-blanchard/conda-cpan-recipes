package:
  name: perl-pod-elemental
  version: !!str 0.103000

source:
  fn: Pod-Elemental-0.103000.tar.gz
  url: http://cpan.metacpan.org/authors/id/R/RJ/RJBS/Pod-Elemental-0.103000.tar.gz
  md5: f28ecbf9e9472cd61b43d1224355903a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-simple 0.96 # Test-Simple
    - perl-extutils-makemaker 6.30 # ExtUtils-MakeMaker
    - perl-test-differences # Test-Differences
    - perl-test-deep # Test-Deep
    - perl-version 0.9901 # version
    - perl-test-pod 1.41 # Test-Pod

  run:
    - perl
    - perl-moose-autobox 0.10 # Moose-Autobox
    - perl-moosex-types # MooseX-Types
    - perl-pod-eventual 0.004 # Pod-Eventual
    - perl-sub-exporter-formethods # Sub-Exporter-ForMethods
    - perl-namespace-autoclean # namespace-autoclean
    - perl-sub-exporter # Sub-Exporter
    - perl-string-truncate # String-Truncate
    - perl-string-rewriteprefix # String-RewritePrefix
    - perl-mixin-linewise # Mixin-Linewise
    - perl-moose 0.90 # Moose
    - perl-moose # Moose

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rjbs/Pod-Elemental
  license: perl_5
  summary: 'work with nestable Pod elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
