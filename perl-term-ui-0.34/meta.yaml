package:
  name: perl-term-ui
  version: !!str 0.34

source:
  #fn: Term-UI-0.34.tar.gz
  #url: http://cpan.metacpan.org/authors/id/B/BI/BINGOS/Term-UI-0.34.tar.gz
  #md5: f3166a9ffc24446f8faabbb267b509d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl 5.18.2

  run:
    - perl 5.18.2

test:
  # Perl 'use' tests
  # imports:

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Term-UI
  license: perl_5
  summary: 'User interfaces via Term::ReadLine made easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
