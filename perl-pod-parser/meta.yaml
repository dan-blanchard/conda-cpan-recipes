package:
  name: perl-pod-parser
  version: !!str 1.62

source:
  fn: Pod-Parser-1.62.tar.gz
  url: http://cpan.metacpan.org/authors/id/M/MA/MAREKR/Pod-Parser-1.62.tar.gz
  md5: 09fbdf2903dc895ffcd851e4b35c4fbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl
    - perl-test-simple 0.6 # Test-Simple

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Pod-Parser
  license: unknown
  summary: 'Modules for parsing/translating POD format documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
