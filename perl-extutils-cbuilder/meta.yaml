package:
  name: perl-extutils-cbuilder
  version: !!str 0.280212

source:
  fn: ExtUtils/ExtUtils-CBuilder-0.280212.tar.gz
  url: http://cpan.metacpan.org/authors/id/A/AM/AMBS/ExtUtils/ExtUtils-CBuilder-0.280212.tar.gz
  md5: ad16f5a5fa2bee90a00b6494b00eb2d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-pod 1.41 # Test-Pod
    - perl-extutils-makemaker 6.30 # ExtUtils-MakeMaker
    - perl-test-simple 0.47 # Test-Simple

  run:
    - perl
    - perl-pathtools 3.13 # PathTools
    - perl-perl-ostype 1 # Perl-OSType

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://search.cpan.org/dist/ExtUtils-CBuilder
  license: perl_5
  summary: 'Compile and link C code for Perl modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
