package:
  name: perl-path-finddev
  version: !!str 0.4.2

source:
  fn: Path-FindDev-0.4.2.tar.gz
  url: http://cpan.metacpan.org/authors/id/K/KE/KENTNL/Path-FindDev-0.4.2.tar.gz
  md5: 8f95bc7519db7d107ca9dad0b7c481cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-path-tiny # Path-Tiny
    - perl-path-isdev # Path-IsDev
    - perl-sub-exporter # Sub-Exporter
    - perl-class-tiny # Class-Tiny

  run:
    - perl
    - perl-path-tiny # Path-Tiny
    - perl-path-isdev # Path-IsDev
    - perl-sub-exporter # Sub-Exporter
    - perl-class-tiny # Class-Tiny

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kentfredric/Path-FindDev
  license: perl_5
  summary: 'Find a development path somewhere in an upper hierarchy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
