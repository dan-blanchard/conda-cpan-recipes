package:
  name: perl-app-mymeta_requires
  version: !!str 0.005

source:
  fn: App-mymeta_requires-0.005.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/App-mymeta_requires-0.005.tar.gz
  md5: c190c987df9bfd6da75a17b108678e2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-object-tiny # Object-Tiny
    - perl-test-deep # Test-Deep
    - perl-class-load # Class-Load
    - perl-getopt-lucid # Getopt-Lucid
    - perl-file-pushd # File-pushd
    - perl-log-dispatchouli # Log-Dispatchouli
    - perl-capture-tiny # Capture-Tiny

  run:
    - perl
    - perl-object-tiny # Object-Tiny
    - perl-getopt-lucid # Getopt-Lucid
    - perl-log-dispatchouli # Log-Dispatchouli
    - perl-class-load # Class-Load

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://metacpan.org/release/App-mymeta_requires
  license: apache_2_0
  summary: 'Extract module requirements from MYMETA files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
