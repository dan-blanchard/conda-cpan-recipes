package:
  name: perl-pod-weaver
  version: !!str 4.006

source:
  fn: Pod-Weaver-4.006.tar.gz
  url: http://cpan.metacpan.org/authors/id/R/RJ/RJBS/Pod-Weaver-4.006.tar.gz
  md5: cb0a5b3a3b0fb739a41f77bcadec896c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-pod 1.41 # Test-Pod
    - perl-ppi # PPI
    - perl-software-license # Software-License
    - perl-test-differences # Test-Differences

  run:
    - perl
    - perl-config-mvp 2 # Config-MVP
    - perl-module-runtime # Module-Runtime
    - perl-moose # Moose
    - perl-datetime # DateTime
    - perl-string-flogger 1 # String-Flogger
    - perl-config-mvp-reader-ini # Config-MVP-Reader-INI
    - perl-list-moreutils # List-MoreUtils
    - perl-params-util # Params-Util
    - perl-pod-elemental 0.100220 # Pod-Elemental
    - perl-namespace-autoclean # namespace-autoclean
    - perl-string-formatter 0.100680 # String-Formatter
    - perl-moose-autobox 0.10 # Moose-Autobox
    - perl-string-rewriteprefix # String-RewritePrefix
    - perl-mixin-linewise 0.103 # Mixin-Linewise
    - perl-config-mvp # Config-MVP
    - perl-log-dispatchouli 1.100710 # Log-Dispatchouli
    - perl-pod-elemental # Pod-Elemental

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rjbs/Pod-Weaver
  license: perl_5
  summary: 'weave together a Pod document from an outline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
