package:
  name: perl-dist-zilla-plugin-run
  version: !!str 0.020

source:
  fn: Dist-Zilla-Plugin-Run-0.020.tar.gz
  url: http://cpan.metacpan.org/authors/id/G/GE/GETTY/Dist-Zilla-Plugin-Run-0.020.tar.gz
  md5: 0fe749b92ad9b2d5e4d8da1c8df9b082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-pod 1.41 # Test-Pod
    - perl-test-loadallmodules 0.021 # Test-LoadAllModules

  run:
    - perl
    - perl-test-file-sharedir # Test-File-ShareDir
    - perl-namespace-autoclean 0.11 # namespace-autoclean
    - perl-dist-zilla 4.200002 # Dist-Zilla
    - perl-moose 1.15 # Moose
    - perl-string-formatter 0.102082 # String-Formatter
    - perl-path-class 0.16 # Path-Class

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Getty/p5-dist-zilla-plugin-run
  license: perl_5
  summary: 'Run external commands at specific phases of Dist::Zilla'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
