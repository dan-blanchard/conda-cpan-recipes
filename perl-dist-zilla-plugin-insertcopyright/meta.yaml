package:
  name: perl-dist-zilla-plugin-insertcopyright
  version: !!str 0.002

source:
  fn: Dist-Zilla-Plugin-InsertCopyright-0.002.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/Dist-Zilla-Plugin-InsertCopyright-0.002.tar.gz
  md5: cc8fc7540e196c417fa717be2514155d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-ppi # PPI
    - perl-dist-zilla # Dist-Zilla
    - perl-path-class # Path-Class
    - perl-moose # Moose
    - perl-file-pushd # File-pushd
    - perl-capture-tiny # Capture-Tiny

  run:
    - perl
    - perl-ppi # PPI
    - perl-dist-zilla # Dist-Zilla
    - perl-moose # Moose

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://metacpan.org/release/Dist-Zilla-Plugin-InsertCopyright
  license: apache_2_0
  summary: 'Insert copyright statement into source code files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
