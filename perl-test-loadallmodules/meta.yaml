package:
  name: perl-test-loadallmodules
  version: !!str 0.022

source:
  fn: Test-LoadAllModules-0.022.tar.gz
  url: http://cpan.metacpan.org/authors/id/K/KI/KITANO/Test-LoadAllModules-0.022.tar.gz
  md5: a44110d3d3b6acec7d00bda2ae1186a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-list-moreutils # List-MoreUtils

  run:
    - perl
    - perl-list-moreutils # List-MoreUtils

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Test-LoadAllModules
  license: perl_5
  summary: 'do use_ok for modules in search path'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
