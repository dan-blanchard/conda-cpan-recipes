package:
  name: perl-module-cpants-analyse
  version: !!str 0.92

source:
  fn: Module-CPANTS-Analyse-0.92.tar.gz
  url: http://cpan.metacpan.org/authors/id/I/IS/ISHIGAKI/Module-CPANTS-Analyse-0.92.tar.gz
  md5: 449a37fb8491af507afac51a317f59ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-nowarnings # Test-NoWarnings
    - perl-test-warn 0.11 # Test-Warn
    - perl-test-deep # Test-Deep

  run:
    - perl
    - perl-yaml # YAML
    - perl-cpan-distnameinfo 0.06 # CPAN-DistnameInfo
    - perl-array-diff 0.04 # Array-Diff
    - perl-class-accessor 0.19 # Class-Accessor
    - perl-file-find-rule-vcs # File-Find-Rule-VCS
    - perl-set-scalar # Set-Scalar
    - perl-software-license 0.003 # Software-License
    - perl-archive-any-lite 0.06 # Archive-Any-Lite
    - perl-io-capture 0.05 # IO-Capture
    - perl-module-extractuse 0.30 # Module-ExtractUse

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cpants.cpanauthors.org
  license: perl_5
  summary: 'Generate Kwalitee ratings for a distribution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
