package:
  name: perl-pod-escapes
  version: !!str 1.06

source:
  fn: Pod-Escapes-1.06.tar.gz
  url: http://cpan.metacpan.org/authors/id/N/NE/NEILB/Pod-Escapes-1.06.tar.gz
  md5: a52b6b0bdfa0ee6b64c015aa92feca1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Pod-Escapes
  license: perl_5
  summary: 'for resolving Pod Elt...gt sequences'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
