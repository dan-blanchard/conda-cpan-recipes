package:
  name: perl-io-storm
  version: !!str 0.04

source:
  fn: IO-Storm-0.04.tar.gz
  url: http://nlp.research.ets.org/~dblanchard/IO-Storm-0.04.tar.gz
  md5: 148167634450030589062e1cec1ee716
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-moo
    - perl-namespace-clean
    - perl-extutils-makemaker
    - perl-json-xs
    - perl-log-log4perl
    - perl-io
    - perl-test-simple
    - perl-test-output
    - perl-test-mockobject

  run:
    - perl
    - perl-moo
    - perl-namespace-clean
    - perl-log-log4perl
    - perl-json-xs
    - perl-io

test:
  # Perl 'use' tests
  # imports:

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dan-blanchard/io-storm
  license: perl_5
  summary: "Perl support for Apache Storm"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
