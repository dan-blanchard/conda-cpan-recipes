package:
  name: perl-locale-maketext-simple
  version: !!str 0.21

source:
  #fn: Locale-Maketext-Simple-0.21.tar.gz
  #url: http://cpan.metacpan.org/authors/id/J/JE/JESSE/Locale-Maketext-Simple-0.21.tar.gz
  #md5: 0a7c5c3b18cf31d764e1631eb9a3d367
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.

  string: pl_5.18.2_1

requirements:
  build:
    - perl 5.18.2

  run:
    - perl 5.18.2

test:
  # Perl 'use' tests
  # imports:

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Locale-Maketext-Simple
  license: perl_5
  summary: 'Simple interface to Locale::Maketext::Lexicon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
