package:
  name: perl-class-factory-util
  version: !!str 1.7

source:
  fn: Class-Factory-Util-1.7.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DR/DROLSKY/Class-Factory-Util-1.7.tar.gz
  md5: aebd79da361b676a7ecd3245fc3d1b3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Class-Factory-Util
  license: perl_5
  summary: 'Provide utility methods for factory classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
