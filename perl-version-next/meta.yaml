package:
  name: perl-version-next
  version: !!str 0.002

source:
  fn: Version-Next-0.002.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/Version-Next-0.002.tar.gz
  md5: abf120bdaa094c3c8fcf23bbb21e34e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-test-exception 0.29 # Test-Exception

  run:
    - perl
    - perl-sub-exporter # Sub-Exporter

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Version-Next
  license: apache_2_0
  summary: 'increment module version numbers simply and correctly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
