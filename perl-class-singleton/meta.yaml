package:
  name: perl-class-singleton
  version: !!str 1.4

source:
  fn: Class-Singleton-1.4.tar.gz
  url: http://cpan.metacpan.org/authors/id/A/AB/ABW/Class-Singleton-1.4.tar.gz
  md5: a18b108ab97e2107cbbe816d2b3e2ee3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Class-Singleton
  license: unknown
  summary: 'Base class for creating singleton objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
