package:
  name: perl-log-message-simple
  version: !!str 0.10

source:
  #fn: Log-Message-Simple-0.10.tar.gz
  #url: http://cpan.metacpan.org/authors/id/B/BI/BINGOS/Log-Message-Simple-0.10.tar.gz
  #md5: 0a0f9df6bc1148cc2aae4cb2882c0664
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl 5.18.2

  run:
    - perl 5.18.2

test:
  # Perl 'use' tests
  # imports:

  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Log-Message-Simple
  license: perl_5
  summary: 'Simplified interface to Log::Message'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
