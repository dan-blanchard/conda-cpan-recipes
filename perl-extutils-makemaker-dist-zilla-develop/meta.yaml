package:
  name: perl-extutils-makemaker-dist-zilla-develop
  version: !!str 0.02

source:
  fn: ExtUtils-MakeMaker-Dist-Zilla-Develop-0.02.tar.gz
  url: http://cpan.metacpan.org/authors/id/D/DO/DOY/ExtUtils-MakeMaker-Dist-Zilla-Develop-0.02.tar.gz
  md5: 5bd36d8e58cf2805226c04126b335f62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/ExtUtils-MakeMaker-Dist-Zilla-Develop
  license: perl_5
  summary: 'create bare-bones Makefile.PL files for use with dzil'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
