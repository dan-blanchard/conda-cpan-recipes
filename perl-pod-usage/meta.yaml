package:
  name: perl-pod-usage
  version: !!str 1.61

source:
  #fn: Pod-Usage-1.61.tar.gz
  #url: http://cpan.metacpan.org/authors/id/M/MA/MAREKR/Pod-Usage-1.61.tar.gz
  #md5: c0262024e2949c0d27b3e8db65d3ad84
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - perl 5.18.2

  run:
    - perl 5.18.2

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Pod-Usage
  license: perl_5
  summary: 'Pod::Usage extracts POD documentation and shows usage information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
