package:
  name: perl-pod-usage
  version: !!str 1.63

source:
  fn: Pod-Usage-1.63.tar.gz
  url: http://cpan.metacpan.org/authors/id/M/MA/MAREKR/Pod-Usage-1.63.tar.gz
  md5: 85b21c0150ec82c5e321a223766ca241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl

  run:
    - perl
    - perl-pathtools 0.82 # PathTools
    - perl-test-simple 0.6 # Test-Simple
    - perl-podlators 3.15 # podlators

# test:
  # By default CPAN tests will be run while "building" (which just uses cpanm
  # to install)

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Pod-Usage
  license: unknown
  summary: 'Pod::Usage extracts POD documentation and shows usage information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
